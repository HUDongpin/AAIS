# Install only after the Owner separately approves KMS pricing and selects
# AAIS_SECRET_SOURCE=kms. The default local-file source does not need network.
[Unit]
Wants=network-online.target
After=network-online.target
